# Local Configuration

This directory is reserved for non-secret local configuration files.

Do not commit credentials, tokens, private keys, `.env` files, subscriber
identifiers, or production network parameters. The unified runner does not
require files from this directory.

