# Optional Tools

This directory is reserved for optional standard-text extraction and qualified
instrument integration helpers.

The core runner uses only the Python standard library and does not require this
directory. The authorized source documents used for internal review are stored
under `standards/official/` and checked against `standards/official/manifest.json`.
Do not publish or redistribute that bundle outside the authorized organization.
Extracted temporary text remains excluded from Git.
