# 服务器例程文件夹说明

服务器账号根目录：

```text
/home/co1750
```

下面只列验证例程会用到的文件夹。

```text
/home/co1750/
├── phase5-ns/                         # 当前主测试台
│   ├── ims/                           # P-CSCF、I-CSCF、S-CSCF 配置
│   └── run-baseline20-20260831-175026/
│       ├── logs/                      # 注册、呼叫、重注册、媒体和错误码日志
│       └── pcaps/                     # NAS、MAC、S1AP 和 EPC 抓包
│
├── verification-evidence/             # 历史证据归档
│                                      # 按 TC、场景和日期保存日志与结果
│                                      # 主要包含 TC-015、TC-018、TC-024、
│                                      # TC-025、TC-030、TC-032、TC-033
│
├── tc005-o5g/                         # TC-005 双 PDN 测试环境
│                                      # UE、eNB、MME、SGWC、SMF、UPF
│                                      # 日志和抓包
├── tc005-mme-debug/                   # TC-005 的 MME 补充调试材料
│
├── srsRAN_4G/                         # LTE/eNB/EPC 环境
│                                      # srsue、srsenb、srsepc、nas_test
│                                      # 对应 TC-001 至 TC-010
│
├── pjproject/                         # pjsua 客户端环境
│                                      # 产生 SIP、RTP、认证和呼叫日志
├── ims-client-skeleton/               # IMS 客户端实验骨架
│                                      # 产生注册、呼叫和状态机日志
├── kamailio/                          # Kamailio SIP/IMS 核心环境
├── pyhss/                             # HSS 和 Diameter 实验环境
├── ims-dns/                           # IMS 域名解析和 P-CSCF 发现配置
├── freeswitch/                        # 会议和媒体服务环境
├── sipp/                              # SIP 场景和流量生成工具
│
├── volte/                             # VoLTE 实验环境
├── volte-src/                         # VoLTE 源码
├── volte-testbed/                     # VoLTE 测试床
├── o5g-local/                         # Open5GS 本地环境
├── lte-ims-interface/                 # LTE 与 IMS 接口实验
├── mariadb/                           # 数据库
├── redis-7.2.15/                      # 缓存服务
├── devlibs/                           # 编译依赖
├── archived-software/                 # 历史软件归档
└── bin/                               # 账号级命令入口
```

## 最常用的三个位置

```text
/home/co1750/phase5-ns/run-baseline20-20260831-175026/logs/
```

主测试日志目录，查 IMS 注册、呼叫、重注册、媒体和错误码行为。

```text
/home/co1750/phase5-ns/run-baseline20-20260831-175026/pcaps/
```

主抓包目录，查 NAS、MAC、S1AP 和 EPC 报文。

```text
/home/co1750/verification-evidence/
```

历史证据归档，按 TC、场景和日期保存。

## 不列在本文的目录

`.cache`、`.bun`、Firefox 缓存、pip 缓存和编译缓存不属于例程材料，不放在
人工说明中。完整绝对路径清单只在
`docs/102a-服务器完整目录清单-20260918.txt` 中查询。
