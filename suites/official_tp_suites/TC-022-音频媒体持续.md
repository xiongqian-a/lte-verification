# TC-022 音频媒体持续

<!-- OFFICIAL_TP_METADATA:START -->
## 0. 元数据

- 官方主规范：`34.229-1 + 26.114`
- 官方章节/TP：`12.12/12.13 SDP/媒体 TP`
- 映射等级：`exact_line_ref`
- 当前证据层级：`LOCAL_PASS`
<!-- OFFICIAL_TP_METADATA:END -->

> 状态：官方 TP 已锚定到 `34.229-1 12.12/12.13` SDP/媒体部分 + `26.114`。本地真实日志字段 PASS，官方媒体性能/容差子例未跑。

## 0. 三层格式执行卡

【用例ID】TC-022 音频媒体持续
【来源】TS 34.229-1 V14.7.0 `MO MTSI Voice Call Successful`（12.12.1 line 4599-4731 / 12.12.2 line 4732-4805）与 `MT MTSI Speech Call`（line 4838-4877）中的 SDP/媒体 TP；TS 26.114 MTSI 媒体要求（line 4698-4708 / 4854-4862 内联引用）。
【测试目的】验证 MTSI 语音呼叫建立后，音频媒体按 SDP 协商结果持续收发 RTP/RTCP，并在常规通话中保持会话完整。
【优先级】P0
【前置条件/SS 环境】
1. UE 已完成 IMS 注册并建立 MO/MT MTSI 语音呼叫。
2. 通话双方协商出至少一种音频编解码（AMR/AMR-WB/EVS）。
3. 抓包环境能记录 RTP/RTCP。
【测试步骤】
1. 建立语音呼叫并确认 SDP 含 `m=audio`。
2. 抓取通话期 RTP/RTCP。
3. 校验实际 RTP payload type 与 SDP `a=rtpmap` 一致。
4. 校验通话期间 RTP 报文持续，无异常中断/EOF。
5. 校验 RTCP 存在或按 SDP `b=RS/RR` 协商（如适用，官方上限 RS <= 4000bps、RR <= 3000bps）。
6. 通话结束，BYE 后媒体流停止。
【预期判据】
- RTP 使用协商好的 IP/端口/SSRC/payload type。
- 通话期间媒体未出现非预期长时间空白/中断（容忍按 26.114/性能规范）。
- RTCP 无持续 NACK/丢包风暴（性能级判据需 SS/仪表）。
- 释放正常。
【通过标准】官方 34.229-1 12.12/12.13 TP Verdict：SIP 建立/释放、SDP 正确交换、媒体行为符合才判 P/F；媒体性能/容差需 SS/仪表。
【失败处理】记录失败步骤、抓包文件、UE 日志，关联缺陷 ID。

## 1. 目的 / 为什么

验证 MTSI 语音呼叫建立后，音频媒体能按 SDP 协商结果持续收发 RTP/RTCP，并在常规通话时长内保持会话完整。

## 2. 官方骨架

- 主来源：`34.229-1 12.12`（MO）与 `12.13`（MT）中的 SDP/媒体 Telephony 判据；`26.114` MTSI 媒体与 RTP/RTCP 要求。
- 正文行号证据：`_extract/34229-1e70_ascii.txt` line 4599-4731（`MO MTSI Voice Call Successful with preconditions`）、line 4732-4805（`MO MTSI Voice Call Successful without preconditions`）、line 4838-4877（`MT MTSI speech call with preconditions`）。媒体/SDP TP 判据见 line 4711-4714、line 4766-4796、line 4854-4867。
- `26.114` 的会话带宽/RTCP 上限引用在 34.229-1 内联见 line 4698-4708、line 4854-4862，对应 `TS 26.114` clause 5.2.1/6.2.5/7.3.1。
- 关联：`24.229` 6.1 SDP 语义，`36.523-1` 核心网承载用模块①覆盖。

### 0.1 官方行号证据

- line 4711-4714：MO TP 目的，验证 SDP 交换、媒体协商、preconditions。
- line 4732-4805：MO Voice Call Successful without preconditions，含 SDP 具体内容段。
- line 4838-4877：MT Speech Call with preconditions，含 TP 判据。
- line 4698-4708 / 4854-4862：26.114 的会话带宽/RTCP 上限内联要求。

## 3. 前置条件

- UE 已完成 IMS 注册并建立 MO/MT MTSI 语音呼叫。
- 通话双方协商出至少一种音频编解码（AMR/AMR-WB/EVS）。
- 抓包环境能记录 RTP/RTCP。

## 4. 验证流程

1. 建立语音呼叫并确认 SDP 含 `m=audio`。
2. 抓取通话期 RTP/RTCP。
3. 校验实际 RTP payload type 与 SDP `a=rtpmap` 一致。
4. 校验通话期间 RTP 报文持续，无中断或异常 EOF。
5. 校验 RTCP 存在或按 SDP `b=RS/RR` 协商（如适用，官方上限 RS<=4000bps、RR<=3000bps）。
6. 通话结束，BYE 后媒体流停止。

## 5. TP Verdict 判据（关键项）

- RTP 使用协商好的 IP/端口/SSRC/payload type。
- 通话期间媒体未出现非预期长时间空白/中断（容忍按 26.114/性能规范）。
- RTCP 无持续 NACK/丢包风暴（性能级判据需 SS/仪表）。
- 释放正常。

## 6. 当前本地证据

```text
python work/tc022_audio_media.py
输入：outputs/tc12-tc13-calls-raw.log
结果：PASS（字段级）
```

## 7. 受限 / L2_REQUIRED

- 当前未按官方媒体测量表判 MOS、端到端时延、丢包、抖动。
- 官方性能判定需 SS/仪表或真实 IMS RTP 媒体链路。

## 8. 执行命令

```text
python work/tc022_audio_media.py
```
