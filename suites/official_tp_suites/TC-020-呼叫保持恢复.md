# TC-020 呼叫保持 / 恢复

<!-- OFFICIAL_TP_METADATA:START -->
## 0. 元数据

- 官方主规范：`34.229-1`
- 官方章节/TP：`15.11 / 15.12`
- 映射等级：`exact_line_ref`
- 当前证据层级：`LOCAL_PASS`
<!-- OFFICIAL_TP_METADATA:END -->

> 状态：官方骨架已锚定到 `34.229-1 15.11 / 15.12` 家族，co1750 pjsua hold/resume 字段级 PASS。真实 MMTel/Hold 业务端到端与官方 `.3.2` 子例未跑。

## 0. 三层格式执行卡

【用例ID】TC-020 呼叫保持 / 恢复
【来源】TS 34.229-1 V14.7.0 `15.11 MO Call Hold / 15.12 MT Call Hold`（含 video 变体 15.11a / 15.12a）；抽取文本行 17443-17455 为通用接口/子例引用，行 20093 为 MTSI TC 修正记录；关联 TS 24.610（Hold）、TS 24.173 V14.3.0 `6.7 Communication Hold (HOLD)`（行 472-473）。
【测试目的】验证 UE 在 MTSI 呼叫中能执行 hold 与 retrieve，SDP 方向、媒体行为、dialog 状态正确且不破坏呼叫。
【优先级】P0
【前置条件/SS 环境】
1. UE 已完成 IMS 注册。
2. UE 已建立 MO 或 MT MTSI 语音呼叫。
3. 远端/SS 支持 hold/resume 的 re-INVITE/UPDATE 与 200 OK。
【测试步骤】
1. 呼叫建立后，UE 发起 hold（re-INVITE/UPDATE，SDP 方向 `sendonly` 或 `inactive`）。
2. 远端回 200 OK；UE ACK。
3. UE 发起 retrieve，SDP 恢复 `sendrecv`。
4. 远端回 200 OK；UE ACK。
5. 校验对话与媒体状态，再释放呼叫。
【预期判据】
- 同一 dialog（Call-ID/From-tag/To-tag 不变）。
- SDP 方向字段正确转换。
- hold 时媒体停止/静默，retrieve 后恢复。
- 无 BYE 误发、无崩溃、无跨对话混乱。
【通过标准】官方 34.229-1 15.11/15.12 的 TP Verdict 判 P/F；当前字段级 PASS 只作研发行为支撑。
【失败处理】记录失败步骤、抓包文件、UE 日志，关联缺陷 ID。

## 1. 目的 / 为什么

验证 UE 在 MTSI 呼叫中能执行 hold 和 retrieve，保持对话/SDP 状态正确，不破坏媒体与后续交互。

## 2. 官方骨架

- 来源：`34.229-1` 15.11 MO Call Hold、15.12 MT Call Hold（含 video 变体 15.11a / 15.12a）。
- 文本抽取记录：`_extract/34229-1e70_ascii.txt`，WLAN/通用引序列行 17443-17455，新 MTSI TC 修正记录行 20093 等。
- 官方 TP 过程原文：`official_tp_suites/_substeps/TC-012-021-official-tp-blocks.json` / `.md`；本用例对应 `15.11.3-15.11.5`、`15.12.3-15.12.5`。逐行核对脚本：`work/verify_34229_call_tp.py`。
- Annex A 默认消息内容：`official_tp_suites/_substeps/TC-012-021-annexA-call-messages.json` / `.md`；hold/resume 所在呼叫的性能与释放消息对应 A.2.7、A.2.8、A.3.1。逐行核对脚本：`work/verify_34229_annexA.py`。
- 关联：`24.610`（Hold），`24.173`（MMTel 补充业务语义）。

### 2.1 24.173 锚点

- 源文本：`C:/Users/co1750/Documents/Codex/2026-09-02/i/_extract/24173.txt`（TS 24.173 V14.3.0）
- HOLD 官方语义入口：`6.7 Communication Hold (HOLD)` 行 472-473，正文明确 “The HOLD service is specified in 3GPP TS 24.610 [8].”
- CONF 官方语义入口：`6.10 Conference (CONF)` 行 478-479，正文明确 “The CONF service is specified in 3GPP TS 24.605 [4].”
- 参考库入口：参考文献列表 `[4] 3GPP TS 24.605` 与 `[8] 3GPP TS 24.610` 行 246 / 256 / 260。
- 结论：`24.173` 本文件只定义 MMTel 服务体系与补充业务归属，不替代 `24.605/24.610` 的全部 HOLD/CONF 行为；本用例仍需按 `34.229-1 15.11/15.12` 与 `24.610` 子例执行。

### 0.1 官方行号证据

- `_extract/34229-1e70_ascii.txt` line 17443-17455：MTSI 15.x 家族通用接口/子例引用。
- `_extract/34229-1e70_ascii.txt` line 20093：MTSI TC 修正记录。
- `_extract/24173.txt` line 472-473：`6.7 Communication Hold (HOLD)`，明确 HOLD 服务由 TS 24.610 规定。

## 3. 前置条件

- UE 已完成 IMS 注册。
- UE 已建立 MO 或 MT MTSI 语音呼叫。
- 远端/SS 支持 hold/resume re-INVITE/UPDATE。

## 4. 验证流程（MO Hold 语义）

1. UE 发起 hold，通常通过 re-INVITE/UPDATE，SDP 中媒体方向 `sendonly` 或 `inactive`。
2. 远端回 200 OK；UE ACK。
3. UE 发起 retrieve，SDP 恢复 `sendrecv`。
4. 远端回 200 OK；UE ACK。

## 5. TP Verdict 判据

- 呼叫仍处于同一 dialog，Call-ID/From-tag/To-tag 不变。
- SDP 方向字段正确转换。
- 媒体在 hold 时停止/接收静默，retrieve 后恢复。
- 不出现 BYE、崩溃、跨对话混乱。

## 6. 当前本地证据

```text
python work/tc020_hold_retrieve.py
输入：outputs/tc-real-pjsua-20260910-174217/tc020-hold/a.log
结果：PASS（字段级）
```

## 7. 受限 / L2_REQUIRED

- 本地为双 UA/简化测试台，非真实 IMS MMTel 业务端到端。
- 官方 15.11/15.12 `.3.2` 子例和真实 MMTel/IMS 端到端未跑。
- `24.173` 的 HOLD 语义已核对到 `6.7`，但 HOLD 具体信令行为以 `24.610` 为准，仍需对接。

## 8. 执行命令

```text
python work/tc020_hold_retrieve.py
```
