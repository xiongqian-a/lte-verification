# TC-033 SIP 定时器超时

<!-- OFFICIAL_TP_METADATA:START -->
## 0. 元数据

- 官方主规范：`24.229 / RFC 3261`
- 官方章节/TP：`定时器`
- 映射等级：`partial`
- 当前证据层级：`LOCAL_PASS`
<!-- OFFICIAL_TP_METADATA:END -->

> 状态：部分锚定于 `24.229` / RFC 3261，本地黑盒超时 PASS。`34.229-1` 无独立定时器一致性 TC，不冒充官方 ID。

## 0. 三层格式执行卡

【用例ID】TC-033 SIP 定时器超时（自研健壮性/行为级）
【来源】RFC 3261 Timer A/B/D/E/F/G/H/I/J；TS 24.229 IMS 呼叫控制中的适用定时器。无独立 34.229-1 一致性 TC，不挂官方一致性 ID。
【测试目的】验证 UE 在 SIP 事务长时间无响应/远端黑洞时，按 SIP 定时器规则超时、重传或终结，不出现永久悬挂/崩溃。
【优先级】P1
【前置条件/SS 环境】
1. UE 已注册 IMS。
2. 能模拟远端黑洞（不回响应）或 2xx/ACK 丢失。
【测试步骤】
1. UE 发起 INVITE/REGISTER。
2. 远端不回响应。
3. 观察 UE 重传/超时行为。
4. 观察会话状态是否终结或进入规范允许状态。
5. 远端恢复后，UE 能继续正常流程或重新发起。
【预期判据】
- 定时器触发符合 RFC/24.229 预期时间窗口。
- 无无限超长悬挂、无资源泄漏。
- 恢复后 UE 行为正常。
【通过标准】行为级：超时/重传/终结行为可复现且无崩溃；不标为 34.229-1 官方一致性 Verdict。
【失败处理】记录失败步骤、抓包文件、UE 日志，关联缺陷 ID。

## 1. 目的 / 为什么

验证 UE 在 SIP 事务长时间无响应/远端黑洞时，按 SIP 定时器规则超时、重传或终结，不出现永久悬挂/崩溃。

## 2. 官方骨架

- 主来源：RFC 3261 Timer A/B/D/E/F/G/H/I/J；`24.229` IMS 呼叫控制中的适用定时器。
- 无独立 34.229-1 一致性 TC；此例归自研健壮性/行为级。
- 行级支撑证据：`official_tp_suites/_substeps/TC-033-24229-sip-timers.json` / `.md`，源文件 `24229.txt`（TS 24.229 V14.17.0）行 10691-10813，含 Table 7.7.1（行 10698 起）的 UE 侧 T1/T2/T4、Timer A/B/C/D/E/F/G/H/I/J/K/L/M/N 和 `64*T1`；逐行核对脚本：`work/verify_24229_sip_timers.py`。

## 3. 前置条件

- UE 已注册 IMS。
- 能模拟远端黑洞（不回响应）或 RR 丢失。

## 4. 验证流程

1. UE 发起 INVITE/REGISTER。
2. 远端不回响应。
3. 观察 UE 重传/超时行为。
4. 观察会话状态是否终结或进入规范允许状态。
5. 远端恢复后，UE 能继续正常流程或重新发起。

## 5. TP Verdict 判据

- 定时器触发符合 RFC/24.229 预期时间窗口。
- 无无限超长悬挂、无资源泄漏。
- 恢复后 UE 行为正常。

## 6. 当前本地证据

```text
python work/tc033_timer_timeout.py
结果：本地黑盒超时 PASS
```

## 7. 受限 / L2_REQUIRED

- 当前是本地模拟响应黑洞，不是 IMS 真实超时/拥塞场景。
- 官方一致性中如需覆盖，应归入 `24.229`/厂家 GoS 或运营商场景，不单列 34.229-1 一致性 ID。

## 8. 执行命令

```text
python work/tc033_timer_timeout.py
```
